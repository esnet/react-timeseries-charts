import * as React from "react";

interface Props {}

const ChartContainer: React.FunctionComponent<Props> = () => (
    <pre style={{ fontSize: 18 }}>{`<ChartContainer> placeholder`}</pre>
);

export default ChartContainer;
