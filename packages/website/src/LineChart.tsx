import * as React from 'react';
import { Route, HashRouter, Switch } from 'react-router-dom';
import App from './App';
import Intro from './Intro';

export const LineChart: React.StatelessComponent = () => {
    return (
      <h2>LineChart example</h2>
    );
}
